apply plugin: 'groovy'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy:2.3.10'
    testCompile 'junit:junit:4.12'
}

sourceSets {
    main {
        groovy {
            srcDirs = ['src/groovy/differences', 'src/groovy/groovy-dev-kit']
        }
    }

    test {
        groovy {
            srcDirs = ['test/groovy']
        }
    }
}

jar {
    manifest {
        attributes 'Main-Class': 'HelloWorld'
    }
}
