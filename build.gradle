subprojects {
    apply plugin: 'groovy'

    repositories {
       mavenLocal()
       mavenCentral()
    }

    dependencies {
        testCompile 'junit:junit:${junitVersion}'
        testCompile "org.codehaus.groovy:groovy-test:${groovyVersion}"
    }

    version = '0.1'
}
